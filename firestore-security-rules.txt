// Add these rules to your Firestore Security Rules

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Email verification collection - allow read/write for OTP operations
    match /emailVerification/{email} {
      allow read, write: if true; // Temporarily allow all access for OTP functionality
      allow delete: if true; // Allow cleanup of expired OTPs
    }
    
    // Your existing rules for other collections...
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
      allow read: if resource.data.isPublic == true;
    }
    
    // Add other collection rules as needed
  }
}